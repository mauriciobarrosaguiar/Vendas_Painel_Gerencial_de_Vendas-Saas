[project]
name = "painelgerencialnorte-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.117.1",
  "numpy",
  "openpyxl",
  "pandas",
  "requests",
  "cryptography",
]
